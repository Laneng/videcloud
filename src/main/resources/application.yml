server:
  port: 8080

#DataSource配置
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/sq_173cms?UseUnicode=true&characterEncoding=utf8&serverTimezone=%2B8
    username: root
    password: root
    type: com.alibaba.druid.pool.DruidDataSource
    # 启动热加载
  devtools:
    restart:
      enabled: true
  servlet:
    multipart:
      max-request-size: 1000MB
      max-file-size: 1000MB